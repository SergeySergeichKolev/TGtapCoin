# 🪙 TapCoin — Telegram Mini App (Tap-to-Earn)

Кликер-игра для Telegram, аналог Notcoin. Пользователи тапают по монетке, зарабатывают очки, соревнуются в таблице лидеров.

## 📁 Структура проекта

```
tap-coin/
├── server.js          # Бэкенд (Node.js, без зависимостей)
├── package.json
├── public/
│   └── index.html     # Фронтенд (всё в одном файле)
└── README.md
```

## 🚀 Как запустить

### 1. Локальный запуск (для тестирования)

```bash
cd tap-coin
node server.js
```

Откроется на `http://localhost:3000` — можно тестировать в браузере.

### 2. Создание Telegram бота

1. Открой **@BotFather** в Telegram
2. Отправь `/newbot`, задай имя и username
3. Скопируй **токен бота** (выглядит как `123456:ABC-DEF...`)
4. Отправь `/newapp` — выбери своего бота
5. Когда спросит URL — укажи URL твоего задеплоенного приложения (см. шаг 3)

### 3. Деплой на сервер (чтобы люди могли играть)

Telegram требует **HTTPS**. Вот самые простые варианты:

---

#### Вариант A: Railway (бесплатно, самый простой)

1. Зарегистрируйся на [railway.app](https://railway.app)
2. Создай новый проект → Deploy from GitHub (или загрузи файлы)
3. Railway автоматически даст HTTPS-ссылку вида `https://tapcoin-xxx.up.railway.app`
4. Эту ссылку указываешь в BotFather при создании Mini App

---

#### Вариант B: VPS (Ubuntu)

```bash
# На сервере:
sudo apt update && sudo apt install -y nodejs npm nginx certbot

# Загрузи файлы проекта на сервер, затем:
cd tap-coin
node server.js &

# Настрой nginx как reverse proxy + SSL:
sudo certbot --nginx -d yourdomain.com
```

Пример конфига nginx (`/etc/nginx/sites-available/tapcoin`):
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

#### Вариант C: Render.com (бесплатно)

1. Зарегистрируйся на [render.com](https://render.com)
2. New → Web Service → загрузи проект
3. Build Command: (оставь пустым)
4. Start Command: `node server.js`
5. Получишь HTTPS-ссылку автоматически

---

### 4. Привязка Mini App к боту

После деплоя с HTTPS-ссылкой:

```
# В BotFather:
/newapp → выбираешь своего бота → вводишь HTTPS URL
```

Или привяжи как кнопку меню:
```
/mybots → выбери бота → Bot Settings → Menu Button → укажи URL
```

### 5. Настройка токена бота (для защиты)

В `server.js` замени:
```js
const BOT_TOKEN = process.env.BOT_TOKEN || '';
```

Или задай через переменную окружения:
```bash
BOT_TOKEN=123456:ABC-DEF node server.js
```

## 🎮 Фичи

- ✅ Тап по монетке с анимациями и частицами
- ✅ Система энергии (восстанавливается со временем)
- ✅ Таблица лидеров
- ✅ Уровни и лиги (Bronze → Silver → Gold → Diamond)
- ✅ Haptic feedback в Telegram
- ✅ Батчинг кликов (отправка на сервер раз в 2 сек)
- ✅ Базовый анти-чит (rate limit, max coins per batch)
- ✅ Работает офлайн для тестирования

## 🔧 Что добавить для продакшена

- [ ] Настоящая БД (PostgreSQL / MongoDB)
- [ ] Redis для кэширования
- [ ] Полная валидация `initData` от Telegram
- [ ] Система рефералов (invite friends)
- [ ] Бусты (увеличение tap power)
- [ ] Daily rewards
- [ ] TON Connect для вывода токенов
- [ ] PM2 или Docker для стабильной работы
